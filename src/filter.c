#include <math.h>
#include <stdint.h>
#include <stdlib.h>

void filter(
    float a[],
    int a_len,
    float b[],
    int b_len,
    int16_t* buffer,
    int buffer_len
) {
    for(int i=0; i<buffer_len; i++) {

    }
}
